spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_jpa
    username: gora
    password: passer123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    show-sql: true
